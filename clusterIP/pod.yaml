apiVersion: v1
kind: Pod
metadata:
  name: CIPpod
  labels: 
    object: pod
spec:
  containers:
    - name: jenkins-con
      image: jenkins/jenkins:latest
      ports:
        - containerPort: 8080
          protocol: TCP
